[global]
daemonize = no
pid = /run/php/php-fpm.pid

[www]
; workers run as the dev user, whose uid the entrypoint matches to the host
user = php-devforge
group = php-devforge
listen = 0.0.0.0:9000
listen.owner = php-devforge
listen.group = php-devforge
pm = ondemand
pm.start_servers = 1
pm.min_spare_servers = 1
pm.max_spare_servers = 1
pm.max_children = 100
pm.max_requests = 500
pm.status_path = /php-fpm-status
ping.path = /php-fpm-ping
request_terminate_timeout = 18000
catch_workers_output = yes
; Logs on stderr & stdout
php_flag[display_errors] = on
php_admin_value[error_log] = /dev/stderr
php_admin_flag[log_errors] = on